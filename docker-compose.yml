services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - API_URL=http://api:8000
    depends_on:
      - api
